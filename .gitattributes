# Line endings are the same on every machine, whatever a contributor's core.autocrlf says. The gate's
# format check requires LF, so a CRLF checkout fails it on every file.
* text=auto eol=lf

# Recovered artifacts, if one is ever forced in by hand.
*.pdb binary
*.BinExport binary
*.gzf binary

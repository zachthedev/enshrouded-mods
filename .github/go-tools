# The gate tools that are not crates.io packages, pinned. One version per tool,
# here and nowhere else: the ci workflow reads this file and the gate's step
# table names the same command.
#
# actionlint is a Go program. It is on neither crates.io nor
# taiki-e/install-action, which is why it cannot live in .github/cargo-tools
# beside the rest.
#
# Nothing bumps these automatically. Dependabot has no ecosystem for a binary
# installed with `go install`, so a human edits this file.
github.com/rhysd/actionlint/cmd/actionlint@v1.7.12
